#!/usr/bin/env python3

import boto3

ddb = boto3.resource('dynamodb', endpoint_url='http://localhost:8000')
table = ddb.Table('cruddur-messages')

response = table.scan()

items = response['Items']
for item in items:
    print(item)
